---
# Override built-in: Next/TS bundler resolves extensionless imports.
id: find-import-file-without-extension
language: TypeScript
severity: warning
message: "(disabled for Next/TS bundler)"
rule:
  # Never match
  all:
    - kind: string_fragment
    - regex: "^$"
